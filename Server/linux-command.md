# LINUX COMMAND

- `ls` = listing (listing of information about files)
- `ls -l` = -l is long display
- `ls -r` = -r reverses the listing
- `pwd` = prints the working dorectory
- `cd` = change directory
- `cd /` = to move to the root directory
- `..` = one directory higher
- `.` = which directory you are in
- `~` = return to home directory

`su` = allows you to temporarily act as a different user
`sl` = to require administrative access
`sudo` = allows a user to execute a command as another user without creating a new shell

`-rw-r--r--`

`rw-` = owner
`r--` = group
`r--` = other

`chmod` (change the modes of access)
`chown` = change the ownership of files and directories
`cat` = display the entire contents of the file
`head, tail`
`head -n 4 <namafile>`

`cp` = copying files
`dd` = command is a utility for copying files or entire partitions at the bit level.
`mv` = to move a file from one location in the filesystem to another
`rm` = delete files and directories
`grep` = is a text that will search input and return lines which contain a match to a given pattern
On the other hand, to find all the lines which contain any non-numeric characters, insert a ^ as the first character inside the brackets. This character negates the characters listed:


14.1 REGEX

`shutdown` = The shutdown command arranges for the system to be brought down in a safe way.
`ifconfig` = command stands for "interface configuration" and is used to display network configuration information.
`ps` = can be used to list processes
`passwd` = command is used to update a user’s password
`vi` = the premier text editor for linux and unix

21 TEXT EDITOR